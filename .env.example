# .env.example — Variáveis para deploy em produção (e referência para dev)
# Copie para .env e preencha os valores. Não commite .env.
#
# Uso: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#      ou stack no Portainer com stack.yml (variáveis no painel).

# --- Obrigatórias em produção ---

# Senha do usuário PostgreSQL (troque em produção)
POSTGRES_PASSWORD=changeme_secure_password

# Secret para assinatura de JWTs (use string longa e aleatória em produção)
JWT_SECRET=changeme_very_long_secret_for_production

# --- Opcionais (PostgreSQL) ---
POSTGRES_DB=sgo
POSTGRES_USER=sgo

# --- Modo gerenciado (Nexus) ---
# Se definida, o chassi opera em modo gerenciado (auto-registro no Nexus).
# Deixe em branco para modo standalone.
# NEXUS_URL=https://nexus.seudominio.com.br

# --- URL pública do chassi ---
# Usado em links (ex.: e-mails). Se não houver proxy com domínio, use http://IP:80 ou deixe em branco.
# CHASSIS_URL=https://sgo.seudominio.com.br

# --- Apenas para stack.yml (Portainer + Traefik) ---
# Domínio para o qual o Traefik roteia (HTTPS com Let's Encrypt)
# DOMAIN=sgo.seudominio.com.br

# Nome da stack (opcional; permite múltiplas instâncias no mesmo Swarm)
# STACK_NAME=sgo

# --- Banco de dados (escolha um) ---

# PostgreSQL (padrão — Docker, VPS, Portainer)
# DATABASE_URL=postgresql://sgo:senha@localhost:5432/sgo

# MySQL (alternativa — ex.: Hostinger, PlanetScale, Railway)
# DATABASE_URL=mysql://sgo:senha@localhost:3306/sgo

# --- Modo Node.js single-process (Hostinger e similares) ---
# Quando true, o backend serve o frontend buildado como arquivos estáticos
# SERVE_STATIC=true
# STATIC_PATH=./public

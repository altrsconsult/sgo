# Dependências
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
*.tsbuildinfo

# Ambiente
.env
.env.local
.env.*.local

# Banco de dados local
*.db
*.db-shm
*.db-wal

# Dados persistentes (volumes Docker)
uploads/
modules_storage/

# Logs
*.log
logs/

# Cache e temporários
.cache/
.turbo/
.vite/
*.tmp

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Deploy packages (gerados localmente)
deploy/*.zip
# Variáveis Portainer (valores reais por cliente) — stacks e exemplo na raiz
stack-env.txt
deploy/stack-env.txt

# Artefatos de módulos — gerados no CI/release; não versionar (Scorecard: Binary-Artifacts)
modules/**/*.zip

# Nexus simulator (apenas dev)
nexus-simulator.html

# Lab de módulos (clientes) — pasta totalmente oculta no repo público
modules-lab/

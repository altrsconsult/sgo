/**
 * Setup global para todos os testes.
 * Importa matchers do jest-dom (toBeInTheDocument, toHaveClass, etc.)
 * e configura cleanup automático após cada teste.
 */
import "@testing-library/jest-dom";

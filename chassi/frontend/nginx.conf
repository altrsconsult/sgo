server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # API e assets de módulos — proxy para o backend (obrigatório no Docker para /api/setup/status etc.)
    location /api/ {
        proxy_pass http://chassi-backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    location /modules-assets/ {
        proxy_pass http://chassi-backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # SPA routing — redireciona qualquer rota para index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache longo para assets com hash no nome
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Sem cache para index.html
    location = /index.html {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}
